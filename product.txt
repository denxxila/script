local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Product Fucker | denxxilaa",
    LoadingTitle = "Product Faker",
    LoadingSubtitle = "by denxxilaa",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "ProductFaker",
        FileName = "Config"
    },
    Discord = {
        Enabled = false,
        Invite = "",
        RememberJoins = true
    },
    KeySystem = false,
})

-- Services
local MarketplaceService = game:GetService("MarketplaceService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- Global Variables
getgenv().ProductLoops = {}
getgenv().GlobalLoop = false
getgenv().ParallelEnabled = false
getgenv().LoopDelay = 0.5
getgenv().ParallelIntensity = 5

-- Fetch Products
local success, developerProducts = pcall(function()
    return MarketplaceService:GetDeveloperProductsAsync():GetCurrentPage()
end)

if not success then
    developerProducts = {}
end

local function FakePurchase(productId)
    MarketplaceService:SignalPromptProductPurchaseFinished(LocalPlayer.UserId, productId, true)
end

-- ══════════════════════════════════════════════════════════════
-- TAB 1: MAIN CONTROLS
-- ══════════════════════════════════════════════════════════════
local MainTab = Window:CreateTab("🏠 Main", 4483362458)

MainTab:CreateSection("📊 Status")

MainTab:CreateParagraph({
    Title = "Product Info",
    Content = "Total Products Found: " .. tostring(#developerProducts) .. "\nGame: " .. game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
})

MainTab:CreateDivider()

MainTab:CreateSection("⚡ Quick Actions")

MainTab:CreateButton({
    Name = "🛒 Buy All Products (1x)",
    Callback = function()
        if #developerProducts == 0 then
            Rayfield:Notify({
                Title = "❌ Error",
                Content = "No products found in this game!",
                Duration = 3
            })
            return
        end
        
        local count = 0
        for _, product in pairs(developerProducts) do
            FakePurchase(product.ProductId)
            count = count + 1
        end
        
        Rayfield:Notify({
            Title = "✅ Success",
            Content = "Purchased " .. count .. " products!",
            Duration = 2
        })
    end,
})

MainTab:CreateDivider()

MainTab:CreateSection("🔄 Auto Loop (Safe Mode)")

MainTab:CreateToggle({
    Name = "Enable Safe Loop",
    CurrentValue = false,
    Flag = "GlobalLoop",
    Callback = function(Value)
        getgenv().GlobalLoop = Value
        
        if Value then
            Rayfield:Notify({
                Title = "🔄 Loop Started",
                Content = "Safe loop is now running",
                Duration = 2
            })
            
            task.spawn(function()
                while getgenv().GlobalLoop do
                    if #developerProducts > 0 then
                        for _, product in pairs(developerProducts) do
                            if not getgenv().GlobalLoop then break end
                            FakePurchase(product.ProductId)
                        end
                    end
                    task.wait(getgenv().LoopDelay)
                end
            end)
        else
            Rayfield:Notify({
                Title = "⏹️ Loop Stopped",
                Content = "Safe loop has been disabled",
                Duration = 2
            })
        end
    end,
})

MainTab:CreateSlider({
    Name = "Loop Delay",
    Range = {0.1, 5},
    Increment = 0.1,
    Suffix = " seconds",
    CurrentValue = 0.5,
    Flag = "LoopDelay",
    Callback = function(Value)
        getgenv().LoopDelay = Value
    end,
})

-- ══════════════════════════════════════════════════════════════
-- TAB 2: EXTREME MODE
-- ══════════════════════════════════════════════════════════════
local ExtremeTab = Window:CreateTab("🔥 Extreme", 4483362458)

ExtremeTab:CreateSection("⚠️ Warning")

ExtremeTab:CreateParagraph({
    Title = "Extreme Mode",
    Content = "This mode uses multi-threading for maximum speed.\nUse with caution - may cause lag!"
})

ExtremeTab:CreateDivider()

ExtremeTab:CreateSection("⚙️ Settings")

ExtremeTab:CreateSlider({
    Name = "Thread Intensity",
    Range = {1, 50},
    Increment = 1,
    Suffix = "x",
    CurrentValue = 5,
    Flag = "Intensity",
    Callback = function(Value)
        getgenv().ParallelIntensity = Value
    end,
})

ExtremeTab:CreateDivider()

ExtremeTab:CreateSection("🚀 Actions")

ExtremeTab:CreateButton({
    Name = "⚡ Instant Burst Buy",
    Callback = function()
        local intensity = getgenv().ParallelIntensity
        
        if #developerProducts == 0 then
            Rayfield:Notify({
                Title = "❌ Error",
                Content = "No products available!",
                Duration = 2
            })
            return
        end
        
        Rayfield:Notify({
            Title = "🚀 Burst Active",
            Content = "Running " .. intensity .. " threads...",
            Duration = 2
        })
        
        for i = 1, intensity do
            task.spawn(function()
                for _, product in pairs(developerProducts) do
                    FakePurchase(product.ProductId)
                end
            end)
        end
    end,
})

ExtremeTab:CreateToggle({
    Name = "Enable Infinite Extreme Loop",
    CurrentValue = false,
    Flag = "ParallelLoop",
    Callback = function(Value)
        getgenv().ParallelEnabled = Value
        
        if Value then
            Rayfield:Notify({
                Title = "🔥 Extreme Mode",
                Content = "Infinite loop started with " .. getgenv().ParallelIntensity .. " threads!",
                Duration = 2
            })
            
            for i = 1, getgenv().ParallelIntensity do
                task.spawn(function()
                    while getgenv().ParallelEnabled do
                        if #developerProducts > 0 then
                            for _, product in pairs(developerProducts) do
                                if not getgenv().ParallelEnabled then break end
                                FakePurchase(product.ProductId)
                            end
                        end
                        RunService.Heartbeat:Wait()
                    end
                end)
            end
        else
            Rayfield:Notify({
                Title = "⏹️ Stopped",
                Content = "Extreme loop disabled",
                Duration = 2
            })
        end
    end,
})

-- ══════════════════════════════════════════════════════════════
-- TAB 3: PRODUCT LIST
-- ══════════════════════════════════════════════════════════════
local ProductTab = Window:CreateTab("📦 Products", 4483362458)

if #developerProducts > 0 then
    ProductTab:CreateSection("📋 Available Products (" .. #developerProducts .. ")")
    
    for index, product in pairs(developerProducts) do
        local prodName = product.Name or "Unknown Product"
        local prodId = product.ProductId
        local prodPrice = product.PriceInRobux or 0
        
        -- Buat section untuk setiap produk
        ProductTab:CreateSection("📦 #" .. index .. " - " .. prodName)
        
        ProductTab:CreateParagraph({
            Title = "Product Details",
            Content = "💰 Price: " .. prodPrice .. " R$\n🆔 ID: " .. prodId
        })
        
        ProductTab:CreateButton({
            Name = "🛒 Buy Once",
            Callback = function()
                FakePurchase(prodId)
                Rayfield:Notify({
                    Title = "✅ Purchased",
                    Content = prodName,
                    Duration = 1.5
                })
            end,
        })
        
        ProductTab:CreateToggle({
            Name = "🔄 Auto Loop This Product",
            CurrentValue = false,
            Flag = "ProductLoop_" .. prodId,
            Callback = function(Value)
                getgenv().ProductLoops[prodId] = Value
                
                if Value then
                    task.spawn(function()
                        while getgenv().ProductLoops[prodId] do
                            FakePurchase(prodId)
                            task.wait(getgenv().LoopDelay)
                        end
                    end)
                end
            end,
        })
        
        ProductTab:CreateButton({
            Name = "📋 Copy Product ID",
            Callback = function()
                setclipboard(tostring(prodId))
                Rayfield:Notify({
                    Title = "📋 Copied!",
                    Content = "ID: " .. prodId,
                    Duration = 1.5
                })
            end,
        })
    end
else
    ProductTab:CreateSection("❌ No Products")
    
    ProductTab:CreateParagraph({
        Title = "Error",
        Content = "No developer products found in this game.\nThis game might not have any products."
    })
end

-- ══════════════════════════════════════════════════════════════
-- TAB 4: SETTINGS & INFO
-- ══════════════════════════════════════════════════════════════
local SettingsTab = Window:CreateTab("⚙️ Settings", 4483362458)

SettingsTab:CreateSection("🎨 Interface")

SettingsTab:CreateButton({
    Name = "🔄 Refresh Products",
    Callback = function()
        local newSuccess, newProducts = pcall(function()
            return MarketplaceService:GetDeveloperProductsAsync():GetCurrentPage()
        end)
        
        if newSuccess then
            developerProducts = newProducts
            Rayfield:Notify({
                Title = "✅ Refreshed",
                Content = "Found " .. #developerProducts .. " products",
                Duration = 2
            })
        else
            Rayfield:Notify({
                Title = "❌ Failed",
                Content = "Could not refresh products",
                Duration = 2
            })
        end
    end,
})

SettingsTab:CreateButton({
    Name = "⏹️ Stop All Loops",
    Callback = function()
        getgenv().GlobalLoop = false
        getgenv().ParallelEnabled = false
        
        for id, _ in pairs(getgenv().ProductLoops) do
            getgenv().ProductLoops[id] = false
        end
        
        Rayfield:Notify({
            Title = "⏹️ Stopped",
            Content = "All loops have been disabled",
            Duration = 2
        })
    end,
})

SettingsTab:CreateDivider()

SettingsTab:CreateSection("ℹ️ Information")

SettingsTab:CreateParagraph({
    Title = "About",
    Content = "Product Faker v1.0\nCreated by denxxila\n\nFollow on TikTok: @denxxila"
})

SettingsTab:CreateParagraph({
    Title = "How to Use",
    Content = "1️⃣ Use Main tab for safe operations\n2️⃣ Use Extreme for maximum speed\n3️⃣ Products tab for individual control\n4️⃣ Adjust delays to prevent detection"
})

SettingsTab:CreateDivider()

SettingsTab:CreateSection("🔗 Links")

SettingsTab:CreateButton({
    Name = "📱 Copy TikTok Username",
    Callback = function()
        setclipboard("denxxila")
        Rayfield:Notify({
            Title = "📋 Copied!",
            Content = "@denxxila",
            Duration = 1.5
        })
    end,
})

-- Load Configuration
Rayfield:LoadConfiguration()

-- Startup Notification
Rayfield:Notify({
    Title = "✅ Loaded Successfully",
    Content = "Product Faker is ready!\nProducts found: " .. #developerProducts,
    Duration = 3
})